''' 
5가지 키워드로 정리하는 핵심 포인트
● 파이썬에서는 값을 만들어 내는 간단한 코드르르 표현식이라고 부릅니다.
● 키워드는 의미가 부여된 특별한 단어로, 사용자가 지정하는 이름에 사용하면 안 됩니다.
● 식별자는 프로그래밍 언어에서 이름을 붙일 때 사용하는 단어입니다.
● 주석은 프로그램을 설명하는 데 사용하며, 프로그램에는 어떠한 영향도 주지 않습니다.
● print()함수는 파니썬의 가장 기본적인 출력  방법으로 괄호 안에 출력하고  싶은 것을 입력해서 사용합니다.
'''

#1 print() 출력
print(
    "#1 print() 출력" "\n"
    '>>> print("Hello Python")' "\n"
    "Hello Python"
)
print("\n")

#2 식별자 구별
print(
    '#2 식별자 구별'
    "a: 식별자" "\n"
    "hello: 식별자" "\n"
    "$hello: 식별자 아님" "\n"
    "→ '_' 외에 특수문자를 사용" "\n"
    "10_hello: 식별자 아님" "\n"
    "→ 숫자로 시작함" "\n"
    "bye: 식별자"
)
print("\n")

#3 print()함수의 print는 무엇인가요
print(
    '#3 print()함수의 print는 무엇인가요' "\n"
    'print는 "식별자"이다.'
)
print("\n")

#4 print()의 함수는 스네이크케이스? 캐멀케이스?
print(
    '#4 print()의 함수는 스네이크케이스? 캐멀케이스?' "\n"
    "→ print()함수는 함수,()가 있으면서 소문자로 시작하기 때문에 스네이크케이스이다."
)
print("\n")

#5 케멀케이스와 스네이크케이스로 형태바꾸기
print(
    '#5 케멀케이스와 스네이크케이스로 형태바꾸기' "\n"
    '예시 → 스네이크케이스 → 매멀케이스'
)
print()
print(
    'hello coding → hello_codig → HelloCoding' "\n"
    'hello python → hello_python → HelloPython' "\n"
    'we are the world → we_are_the_world → WeAreTheWorld' "\n"
    'create output → create_output → CreateOutput' "\n"
    'create reques t→ create_request → CreateRequest' "\n"
    'init server → init_server → InitServer' "\n"
    'init matrix → init_matrix → InitMatrix'
)